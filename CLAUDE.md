# Project Notes for Claude Code

## Build Process

**IMPORTANT:** Never run `./build.sh` when working on this site. The user runs `rheo watch` in another terminal, which automatically rebuilds the site when files change. You can directly check the output in `build/html/`.
